[build]
  command = "pnpm --filter @eclipse/web build"
  publish = "apps/web/dist"

[[redirects]]
  from = "/api/*"
  to = "https://eclipsegames-api.duckdns.org/api/:splat"
  status = 200
  force = true
