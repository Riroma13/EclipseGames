/** Rubric DTOs are deliberately assembled here, rather than exposing database rows. */
export function toRubricDto(value:any) { return value; }
export function toGroupRubricDto(value:any) { return value; }
