# SPEC-0042 Tasks — M11 Narrative, Events, Challenges, and Minigames

## Expected Change Surface

- **Database:** `apps/api/drizzle/0019_m11_narrative_progress.sql`, `apps/api/src/db/migrations.ts`, `apps/api/src/db/schema.ts`.
- **Backend/domain:** new `apps/api/src/narrative/{catalogue,repository,service,mapper,routes}.ts` plus focused tests; server registration in `apps/api/src/server.ts`; shared DTOs in `packages/contracts/src/index.ts` only if required.
- **Adapters:** narrow challenge/minigame terminal-state ports and Projection composition/mapper/route changes in `apps/api/src/projection/*`; no writes to source game tables.
- **Web:** `apps/web/src/main.tsx`, workspace shell/navigation and new narrative workspace components/API/tests, Projection components/API/tests, styles, and built-artifact tests under `apps/web/e2e/`.
- **Forbidden surface:** `DESIGN.md`, `NARRATIVE-CONTENT-DRAFT.md`, generic events/history, grades/XP/RT/energy/gems/behaviour/roster/calendar/avatar/boutique/term-close/source-game mutations, deployment, and runtime tracking.

## Read Order

1. `DESIGN.md`, `NARRATIVE-CONTENT-DRAFT.md` (canon and contract).
2. `AGENTS.md`, `apps/api/src/db/{migrations,schema,migrate}.ts`, `apps/api/src/services/transactions.ts`.
3. Existing game/calendar ownership and terminal-state routes/repositories; `apps/api/src/server.ts`.
4. `packages/contracts/src/index.ts`, projection composer/mapper/routes and tests.
5. `apps/web/src/{main.tsx,workspace,events,projection,game}` and `playwright.config.ts`.

## Bounded Task Slices

- [x] **T1 — Migration and schema foundation.** Objective: add empty migration 0019 and typed tables for aggregate head, nine event rows, and durable command receipts. Surface: the three named tables, full owner/group/year lineage FKs and indexes, aggregate revision/checks, mechanic/status constraints, receipt uniqueness and exact response fields, migration registration after 0018, Drizzle schema. Constraints: no backfill or generic-row inference; `PRAGMA foreign_keys=ON`; archive retention follows aggregate lifetime. Verify migration structure, lineage isolation, empty initialization, and rollback tests. Depends on none. Evidence: `pnpm exec vitest run apps/api/src/db/migrate.test.ts -t "rejects invalid populated avatar data before rebuilding 0018|0019 narrative progress migration"` — 4 passed, 7 skipped; API typecheck passed in the prior bounded run.
- [x] **T2 — Canonical catalogue and pure progression.** Objective: encode all nine approved keys/content exactly and derive only `BLOCKED|AVAILABLE|COMPLETED`. Surface: `narrative/catalogue.ts` and domain/service tests. Constraints: catalogue is sole identity/content source; terms, prerequisites, clue bounds, required/optional/none mechanics, and no persisted state column; authoritative group/year progression permits catch-up only after term eligibility. Verify parameterized catalogue equality, state derivation, term/archive gates, and no cross-domain mutation. Depends on T1. Evidence: `pnpm exec vitest run apps/api/src/narrative/domain.test.ts` — 1 file, 4 tests passed; `pnpm --filter @eclipse/api exec tsc --noEmit` — passed.
- [x] **T3 — Repository, CAS, adapters, and durable commands.** Objective: implement full-lineage reads/writes and transactional START, REVEAL_NEXT_CLUE, LINK, COMPLETE. Surface: `repository.ts`, `service.ts`, `mapper.ts`, transaction integration, challenge/minigame read-only ports. Constraints: `BEGIN IMMEDIATE`; head CAS exactly once; stale revision is 409 before command checks; REQUIRED/OPTIONAL/NONE terminal rules; challenge `COMPLETED`, minigame `ENDED`; canonical UUID-v4 idempotency fingerprint/replay; failed attempts roll back; archived years are read-only except exact replay. Verify concurrency, response-loss replay, same-key races, changed reuse, scope isolation, pre-receipt rollback, and zero source writes. Depends on T1–T2. Evidence: `pnpm exec vitest run apps/api/src/narrative/service.test.ts` — 1 file, 5 tests passed; covers persistence round-trip, lineage isolation, CAS/stale conflicts, replay and changed-key reuse, archive rejection, pre-receipt rollback, and zero non-narrative/source mutations.
- [x] **T4 — Authenticated API adapter.** Objective: expose GET state and four command routes with strict validation and safe error/`no-store` contracts. Surface: `narrative/routes.ts`, `server.ts`, contracts, API integration tests. Constraints: resolve authenticated teacher-owned complete lineage first; 404 for foreign tuples; exact payload allowlists and response bodies; never leak receipts, fingerprints, private clues, or IDs. Verify all validation, ownership, archive, conflict, replay, and read contracts. Depends on T3. Evidence: `pnpm exec vitest run apps/api/src/narrative/routes.integration.test.ts` — 1 file, 3 tests passed; `pnpm --filter @eclipse/api exec tsc --noEmit` — passed. Covers authentication/ownership/no-store, strict DTO and safe errors, required challenge flow/source immutability, optional minigame and none rejection semantics, replay/conflicting key, stale CAS, archived read-only, and fail-closed invalid lineage/adapter reads.
- [x] **T5 — Projection allowlist adapter.** Objective: add NARRATIVE priority and lifecycle-specific safe serialization. Surface: projection composer/ports/mapper/routes and tests. Constraints: AVAILABLE is exactly title/term/ordinal/collective counts with no key/state/clue text even after teacher reveal; COMPLETED may expose only approved revealed clues and marker/progress; no private source reads. Verify all nine parameterized privacy cases and priority behavior. Depends on T2–T4. Evidence: `pnpm exec vitest run apps/api/src/projection/narrative-composer.test.ts` — 1 file, 12 tests passed; API typecheck passed after centralizing `showStudent` in `ProjectionPriorityState` and `projectionState`.
- [x] **T6 — Teacher narrative workspace.** Objective: add Spanish `/narrative` navigation and teacher-private timeline/current scene, clue/link/completion controls, loading/retry/conflict/read-only states, and retained idempotency keys across unknown outcomes. Surface: `main.tsx`, workspace shell/context, narrative API/components/tests/styles. Constraints: archived read-only; controls never alter Projection output; existing pages remain intact and client clears keys only per Design. Verify focused web/API integration, keyboard/focus, reload, progression, and conflict refresh. Depends on T4. Evidence: `pnpm exec vitest run apps/web/src/workspace/NarrativeWorkspace.test.tsx` — exit 0; 1 file, 3 tests passed; `pnpm --filter @eclipse/web exec tsc --noEmit` — exit 0; passed.
- [x] **T7 — Built-artifact acceptance — COMPLETE.** Objective: prove the real built journey. Surface: `apps/web/e2e/spec-0042-narrative.spec.ts` and only required harness fixtures/config adjustments. Constraints: use built artifact, not dev mocks; prove teacher progression, links, replay/reload, archived read-only, Projection clue privacy and post-completion reveal. Depends on T5–T6. Evidence: focused API, web, typecheck, and Playwright evidence is recorded in `VERIFY.md`; cleanup removed diagnostics, diagnostic env flags/logging/helpers, and the `location.key` mechanism while retaining the permanent Projection regression.

## Critical Terra Verification Gate: APPROVED

## Slice 3 status

**PASS** — T3, T4, T5, and T6 are complete and their focused evidence is green.

## Slice 4 status

**COMPLETE** — T7 built-artifact acceptance is complete; exact focused evidence and coverage are recorded in `VERIFY.md`.
